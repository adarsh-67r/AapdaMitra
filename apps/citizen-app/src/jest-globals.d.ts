// TypeScript 6 does not pull @types/jest into the program automatically here,
// and setting compilerOptions.types to add it would switch off automatic
// inclusion of every other @types package. A reference is additive.
/// <reference types="jest" />
