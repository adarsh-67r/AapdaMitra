# URL of the AapdaMitra FastAPI backend. This is the only variable the web app
# needs — it talks to the backend for everything (auth, alerts, reports,
# resources, allocation) and no longer uses Supabase client libraries directly.
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
